generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Cameras {
  cameraname   String
  id           Int    @default(autoincrement()) @id
  manufacturer String
  serialnumber String
}